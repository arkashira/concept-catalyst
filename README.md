# Concept Catalyst
A simple Python project that generates testable hypotheses from a vague idea.

## Usage
1. Run `python -m pytest` to test the implementation.
2. Use the `generate_hypotheses` function to generate hypotheses from an idea.
3. Use the `get_editable_templates` function to get editable templates for customer segments and target problems.
